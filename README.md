# Chat App on [Stream API](https://getstream.io/chat/docs/)

Setup:

- copy

``` shell
 cp ./client/.env.example ./client/.env
```
``` shell
 cp ./server/.env.example ./server/.env
```

- run ```npm i && npm start``` for both client and server side
